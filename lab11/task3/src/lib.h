#include <stdio.h>
#include <stdlib.h>

//size of array
#define N 11

//finds the size of max sequence
int size_of_seq(const int* arr);

//getting the max seq
int* max_seq(const int* arr, int count_of_seq);
